---
doc_id: "canonical.result"
doc_kind: "canonical_result"
canonical: true
priority: 100
updated_at: "2026-04-10T18:28:21+07:00"
git_sha: "53d7480"
tags: ["benchmark", "result", "verdict", "evidence"]
depends_on: []
bootstrap: true
---

# Kết quả gần nhất của hệ thống

## 1. Nguồn kết quả

Tài liệu này phản ánh round benchmark đã chạy thật vào `2026-04-10 18:28` (Asia/Saigon), tại commit `53d7480`, dựa trên:

- `routeIntelligenceVerdictSmoke`
- `scenarioBatchCertification`
- `scenarioBatchRealisticHcmc`
- `batchIntelligenceCertificationSummary`
- `repoIntelligenceCertificationSummary`
- `publicResearchBenchmarkCertificationSummary`

Round này tách rõ ba loại trạng thái:

- pass hoặc fail do logic route thật
- blocked do thiếu dataset benchmark công khai
- timeout ở lane sâu, không dùng làm nguồn kết luận cuối

## 2. Verdict tổng quan hiện tại

- `AI Verdict = YES`
- `Routing Verdict = PARTIAL`
- `Confidence = MEDIUM`
- `Claim Readiness = INTERNAL_ONLY`

Ý nghĩa hiện tại:

- hệ thống có AI thật trên hot path
- smoke lane vẫn pass sau các thay đổi benchmark gần đây
- clean-regime đã có nhiều bucket dương
- nhưng route quality tổng thể vẫn chưa đủ để thoát `PARTIAL`

## 3. Smoke đã verify trong round này

### Route AI certification smoke

- Lane: `route-ai-certification-smoke`
- Verdict: `PASS`
- Scenario group: `CLEAR`
- Dispatch P95/P99: `5.4ms / 6.7ms`
- Gain/completion/deadhead/post-drop:
  - `+2.2% / +4.03pp / -1.86pp / +0.89pp`
- Safety pass: `true`
- Dominant stage: `candidateGeneration`

### Route intelligence verdict smoke

- `AI = YES`
- `Routing = PARTIAL`
- `Confidence = MEDIUM`
- `Claim Readiness = INTERNAL_ONLY`

Diễn giải:

- smoke vẫn xác nhận AI có mặt thật trên đường chọn plan
- smoke không cho thấy hệ thống tụt xuống `NO`
- nhưng smoke vẫn chưa đủ để nâng verdict tổng thể

## 4. Certification lane nói gì

### Repo intelligence certification

- Lane: `repo-intelligence-certification`
- Verdict: `FAIL`

Gate breakdown:

- `Correctness = PASS`
- `Latency = FAIL`
- `Route Quality = FAIL`
- `Continuity = FAIL`
- `Stress/Safety = FAIL`
- `Auxiliary = FAIL`

Các blocker chính của lane này:

- `LIGHT_RAIN` đang thiếu sample trong certification round hiện tại
- `DEMAND_SPIKE` chưa qua route-quality và continuity gate
- `HEAVY_RAIN` vẫn gãy mạnh, trong đó dispatch P95 trung bình `201.3ms`
- `trackA` global gain summary chưa có, nên auxiliary gate fail

### Legacy reference trong certification

- latest overall gain: `-1.74%`
- latest completion delta: `-0.94pp`
- latest deadhead delta: `+1.87pp`

Điểm này rất quan trọng:

- certification lane hiện chưa đủ mạnh để kết luận Omega thắng Legacy một cách ổn định
- vì vậy verdict tổng thể chưa thể nâng lên `YES with caveat`

## 5. Scenario certification: bucket nào mạnh, bucket nào yếu

### Bucket đang tốt

- `CLEAR`
  - mean gain `+2.6%`
  - completion `+3.2pp`
  - deadhead `-2.8pp`
- `RUSH_HOUR`
  - mean gain `+1.1%`
  - completion `+1.1pp`
  - deadhead `-2.2pp`
- `SHORTAGE`
  - mean gain `+0.7%`
  - completion `+0.8pp`
  - deadhead `-3.9pp`

### Bucket mixed

- `DEMAND_SPIKE`
  - mean gain `+0.4%`
  - completion `-0.7pp`
  - deadhead `-2.1pp`
  - một seed vẫn ra `BASELINE_BETTER`

### Bucket gãy rõ

- `HEAVY_RAIN`
  - mean gain `-13.0%`
  - completion `-8.4pp`
  - deadhead `+20.0pp`
  - post-drop hit tụt rất mạnh
  - đây vẫn là blocker lớn nhất của route quality

## 6. Realistic HCMC round nói gì

Round `scenarioBatchRealisticHcmc` mới nhất cho thấy:

### Các bucket tích cực

- `dinner-peak`
  - verdict `AI_BETTER`
  - completion `+2.3pp`
  - deadhead `-11.1pp`
  - post-drop hit `+15.6pp`
- `weekend-demand-spike`
  - verdict `AI_BETTER`
  - completion `+2.3pp`
  - deadhead `-8.2pp`
  - post-drop hit `+6.5pp`

### Các bucket mixed

- `morning-off-peak`
  - gain gần hòa
  - deadhead tốt hơn
  - nhưng completion kém hơn `Legacy`
- `lunch-peak`
  - deadhead tốt hơn
  - post-drop tốt hơn
  - nhưng on-time kém hơn
- `afternoon-off-peak`
  - completion và deadhead tốt hơn
  - nhưng wait-style behavior còn xấu
- `shortage-regime`
  - completion và deadhead tốt hơn nhẹ
  - nhưng vẫn còn borrowed/fallback nặng

### Các bucket baseline-better

- `night-off-peak`
  - deadhead xấu hơn `Legacy`
  - on-time và cancel cũng xấu hơn
- `heavy-rain-lunch`
  - gain `-13.7%`
  - completion `-6.3pp`
  - deadhead `+20.2pp`
  - post-drop hit `-84.9pp`

Kết luận từ realistic HCMC:

- route trong môi trường thuận lợi đã có nhiều dấu hiệu thông minh hơn
- nhưng off-peak yếu và heavy-rain lunch vẫn kéo verdict tổng thể xuống

## 7. Batch intelligence hiện tại

- Lane: `batch-intelligence-certification-certification`
- Verdict: `PASS`
- `batch2Samples = 1`
- `utilityVsSingleMean = +0.21`
- `utilityVsExtensionMean = +0.12`
- `inflationRate = 0.0`

Điểm cần hiểu đúng:

- lane này xác nhận pipeline ghép đơn không đang tăng batch count một cách giả tạo
- nhưng sample còn quá nhỏ để coi là proof mạnh ở cấp nghiên cứu
- đây là tín hiệu tích cực, chưa phải bằng chứng đủ rộng

## 8. Public research benchmark hiện tại

- Lane: `public-research-benchmark-certification`
- Verdict: `FAIL`

Nhưng đây là `BLOCKED_BY_DATASET`, không phải fail do route logic đã đo xong.

Trạng thái family:

- `solomon`: thiếu compare artifacts
- `homberger`: thiếu compare artifacts
- `li-lim-pdptw`: thiếu compare artifacts

Nguyên nhân gốc:

- thư mục dataset công khai chưa có dữ liệu thật
- vì vậy public research proof chưa thể coi là hoàn tất

## 9. Trạng thái AI proof

Trong round này, phần có thể kết luận chắc chắn là:

- AI hot path vẫn hiện diện thật
- smoke proof vẫn sống
- batch lane vẫn tạo tín hiệu dương

Điều chưa thể dùng làm claim mạnh:

- ablation certification sâu đã bị timeout ở lần chạy này
- public research benchmark chưa sẵn dataset
- vì vậy chưa có đủ bằng chứng để nâng `claim_readiness`

## 10. Điểm mạnh và điểm yếu còn lại

### Điểm mạnh

- smoke lane khỏe
- `CLEAR`, `RUSH_HOUR`, `SHORTAGE` đang có tín hiệu tốt
- một số slice HCMC như `dinner-peak` và `weekend-demand-spike` cải thiện rõ
- batch admission hiện không cho thấy batch inflation

### Điểm yếu

- `Routing Verdict` vẫn là `PARTIAL`
- `HEAVY_RAIN` vẫn là bucket gãy lớn nhất
- `night-off-peak` và một phần `morning-off-peak` chưa sạch
- public research proof đang bị chặn bởi dataset
- certification lane còn fail do thiếu `LIGHT_RAIN` sample và thiếu `trackA` summary

## 11. Kết luận ngắn

Trạng thái đúng của hệ thống sau round benchmark này là:

- đủ để nói hệ thống có AI thật
- đủ để nói route đã có nhiều bucket clean-regime hành xử tốt hơn trước
- chưa đủ để nói route core đã qua ngưỡng `YES with caveat`
- chưa đủ để dùng public research benchmark làm proof học thuật, vì dataset còn thiếu

Nói ngắn gọn hơn:

- `AI`: có thật
- `route`: đang tiến bộ nhưng chưa đủ mạnh toàn cục
- `batching`: có tín hiệu đúng hướng nhưng proof còn hẹp
- `public benchmark`: đang blocked bởi dữ liệu
