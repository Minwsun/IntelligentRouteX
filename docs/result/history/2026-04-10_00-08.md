---
doc_id: "canonical.result"
doc_kind: "canonical_result"
canonical: true
priority: 100
updated_at: "2026-04-10T00:08:26+07:00"
git_sha: "1ddb17b"
tags: ["benchmark", "result", "verdict", "evidence"]
depends_on: []
bootstrap: true
---

# Kết quả gần nhất của hệ thống

## 1. Nguồn kết quả

Tài liệu này phản ánh trạng thái benchmark đã được verify gần nhất tính tới `2026-04-09 23:58` (Asia/Saigon), dựa trên:

- `routeIntelligenceVerdictSmoke`
- `route-ai-certification-smoke`
- artifact full-scope gần nhất của `scenarioBatchRealisticHcmc` từ phase trước

Mục tiêu của file này là ghi đúng evidence đã chạy thật. Nếu một slice chưa được rerun trong lượt mới nhất, file này sẽ nói rõ thay vì ngầm xem nó là “đã cập nhật”.

## 2. Verdict tổng quan hiện tại

- `AI Verdict = YES`
- `Routing Verdict = PARTIAL`
- `Confidence = MEDIUM`
- `Claim Readiness = INTERNAL_ONLY`

Ý nghĩa hiện tại:

- hệ thống có AI thật trên hot path
- nhát cắt bigdata-first mới không làm mất certification smoke
- nhưng route quality tổng thể vẫn chưa đủ để bỏ nhãn `PARTIAL`

## 3. Smoke đã verify ở nhát cắt mới nhất

### Route AI certification smoke

- Lane: `route-ai-certification-smoke`
- Verdict: `PASS`
- Scenario group: `CLEAR`
- Dispatch P95/P99: `9.2ms / 23.5ms`
- Gain/completion/deadhead/post-drop:
  - `+1.4% / +3.80pp / -5.43pp / +0.19pp`
- Safety pass: `true`
- Dominant stage: `candidateGeneration`

Điểm quan trọng nhất của lần chạy này:

- nhát cắt `OSM/OSRM + self-derived traffic surrogate` vẫn giữ được smoke certification
- root cause fail trước đó không nằm ở benchmark, mà ở việc surrogate traffic trong `CLEAR` đang làm route quá dè chừng
- sau khi hiệu chỉnh surrogate cho `CLEAR`, smoke certification đã quay lại `PASS`

### Route intelligence verdict smoke

- `AI = YES`
- `Routing = PARTIAL`
- `Confidence = MEDIUM`
- `Claim Readiness = INTERNAL_ONLY`

Diễn giải:

- kiến trúc AI vẫn được benchmark công nhận là AI thật
- repo lane smoke vẫn pass
- nhưng route quality toàn cục vẫn chưa đủ mạnh để đổi verdict tổng

## 4. Bằng chứng AI hiện tại

Các lane AI đang có mặt trên hot path:

- ETA
- late risk
- cancel risk
- route value
- batch value
- continuation outcome
- stress rescue
- driver positioning
- graph affinity, graph shadow, future cell value
- neural route prior

Ablation smoke gần nhất cho thấy:

- `NO_BATCH_VALUE`: `FULL_BETTER`
- `NO_POSITIONING_MODEL`: `FULL_BETTER`
- `NO_NEURAL_PRIOR`: `MIXED`
- `NO_CONTINUATION`: `MIXED`
- `NO_STRESS_AI_GATE`: `MIXED`

Kết luận đúng ở thời điểm này:

- hệ thống có AI thật
- các lane chính đã có ảnh hưởng đo được
- nhưng vẫn còn lane cần calibration thêm để tạo lợi thế ổn định hơn, nhất là continuation và stress-related behavior

## 5. Nhát cắt bigdata-first vừa hoàn thành tạo ra điều gì

Slice mới đã thêm các thành phần thật vào route core:

- `OSM/OSRM`-style road graph abstraction
- `TrafficFeatureEstimator`
- các feature:
  - `pickupFrictionScore`
  - `dropReachabilityScore`
  - `corridorCongestionScore`
  - `zoneSlowdownIndex`
  - `travelTimeDriftScore`
  - `trafficUncertaintyScore`

Các feature này đã đi vào:

- route scoring
- continuation/landing adjustment
- fact emission cho candidate evidence
- runtime bootstrap và feature store

Điểm đúng cần hiểu:

- đây là bước đầu của `bigdata-first`
- phase hiện tại mới là `open-source surrogate + feature wiring`
- chưa phải full streaming traffic backbone từ telemetry/Kafka/Flink

## 6. Evidence rộng hơn hiện vẫn nói gì

Full `scenarioBatchRealisticHcmc` chưa được rerun lại trong chính nhát cắt này, nên trạng thái rộng hơn vẫn bám theo evidence gần nhất trước đó:

- `heavy-rain lunch` vẫn là blocker lớn nhất
- `night off-peak` và `shortage regime` vẫn chưa sạch
- `morning off-peak` vẫn chưa ổn định đủ đẹp

Vì vậy:

- smoke clean-regime hiện đang tốt và pass
- nhưng evidence rộng hơn vẫn chưa cho phép claim rằng route đã đủ mạnh toàn diện

## 7. Truth-layer note

Truth layer hiện vẫn được giữ đúng hướng:

- `openPickupDemand` chỉ đại diện cho demand pickup còn mở thật
- `committedPickupPressure` giữ riêng áp lực pickup đã commit

Điều này cần được hiểu như một guard của hệ thống:

- không mô tả stale pickup hotspot như bug chắc chắn còn mở
- nhưng vẫn phải giữ regression coverage để continuation, positioning, graph foresight và traffic surrogate không vô tình kéo hotspot cũ quay lại

## 8. Điểm mạnh và điểm yếu còn lại

### Điểm mạnh

- AI hot path có thật
- smoke certification đang pass
- open-source-first road graph slice đã vào route core
- candidate facts đã giàu hơn cho training và audit

### Điểm yếu

- `Routing Verdict` vẫn là `PARTIAL`
- full bigdata-first streaming spine chưa được nối thật
- heavy-rain vẫn là blocker route-quality lớn nhất
- một số lane cần được retrain hoặc recalibrate trên feature mới để delta rộng hơn

## 9. Kết luận ngắn

Trạng thái đúng của hệ thống hiện tại là:

- AI: có thật
- smoke: đang pass sau nhát cắt bigdata-first
- kiến trúc: đã tiến sang hướng `AI-first + bigdata-first + open-source-first`
- route quality tổng thể: vẫn chưa xong, vì `Routing Verdict` còn `PARTIAL`

Nếu session tiếp theo cần biết nên làm gì, thì câu trả lời đúng là:

1. biến surrogate traffic hiện tại thành streaming feature backbone thật
2. recalibrate route lanes trên feature mới
3. sau đó mới quay lại dọn các bucket route-quality còn yếu như heavy-rain và shortage
